{
  "name": "@rajaku-printing/notification-worker",
  "version": "0.1.0",
  "private": true,
  "description": "WhatsApp notification worker for Rajaku Printing (Baileys + polling loop against Go backend). Non-blocking async dispatcher — backend inserts jobs into notification_jobs, this worker claims + sends + reports back.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@whiskeysockets/baileys": "^6.7.9",
    "pino": "^9.5.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
